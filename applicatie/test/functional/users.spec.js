'use strict'

const { skip, test, before, beforeEach, after, afterEach } = use('Test/Suite')('Users')

skip('Add a new user', async ({ assert }) => {})

skip('Get all users', async ({ assert }) => {})

skip('Edit a existing user', async ({ assert }) => {})

skip('Validate a new user', async ({ assert }) => {})

skip('Validate a existing user', async ({ assert }) => {})

skip('Delete a existing user', async ({ assert }) => {})
