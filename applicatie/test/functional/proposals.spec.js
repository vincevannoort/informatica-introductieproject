'use strict'

const { test, before, beforeEach, after, afterEach } = use('Test/Suite')('Proposals')

test('Get all proposals', async ({ assert }) => { assert.isTrue(false) })

test('Add a new proposal', async ({ assert }) => { assert.isTrue(false) })

test('Edit a existing proposal', async ({ assert }) => { assert.isTrue(false) })

test('Validate a new proposal', async ({ assert }) => { assert.isTrue(false) })

test('Validate a existing proposal', async ({ assert }) => { assert.isTrue(false) })

test('Attach a contact to an proposal', async ({ assert }) => { assert.isTrue(false) })

test('Attach multiple contacts to an proposal', async ({ assert }) => { assert.isTrue(false) })

test('Calculate insight after adding a new contact to an proposal', async ({ assert }) => { assert.isTrue(false) })

test('Calculate insight after changing contact information of an contact attached to an proposal', async ({ assert }) => { assert.isTrue(false) })