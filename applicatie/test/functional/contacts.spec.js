'use strict'

const { skip, test, before, beforeEach, after, afterEach } = use('Test/Suite')('Contacts')

skip('Add a new contact', async ({ assert }) => {})

skip('Get all contacts', async ({ assert }) => {})

skip('Edit a existing contact', async ({ assert }) => {})

skip('Validate a new contact', async ({ assert }) => {})

skip('Validate a existing contact', async ({ assert }) => {})

skip('Add social media to an existing contact', async ({ assert }) => {})

skip('Upload a foto to a contact profile', async ({ assert }) => {})

skip('Add notes to a contact profile', async ({ assert }) => {})

skip('Delete a existing contact', async ({ assert }) => {})