const { skip } = use('Test/Suite')('Contacts')

skip('Add a new contact', async () => {})

skip('Get all contacts', async () => {})

skip('Edit a existing contact', async () => {})

skip('Validate a new contact', async () => {})

skip('Validate a existing contact', async () => {})

skip('Add social media to an existing contact', async () => {})

skip('Upload a foto to a contact profile', async () => {})

skip('Add notes to a contact profile', async () => {})

skip('Delete a existing contact', async () => {})
