'use strict'

const { test, before, after } = use('Test/Suite')('Relations')
const Database = use('Database')

const Relation = use('App/Models/Relation')

before(async () => {

})

after(async () => {

})

test('Get all relations', async ({ assert }) => {})

test('Add a new relation', async ({ assert }) => {})

test('Edit a existing relation', async ({ assert }) => {})

test('Validate a new relation', async ({ assert }) => {})

test('Validate a existing relation', async ({ assert }) => {})

test('Attach a contact to an relation', async ({ assert }) => {})

test('Add a new proposal', async ({ assert }) => {})

test('Calculate total insight based on proposals', async ({ assert }) => {})

test('Seperate relations in 3 groups', async ({ assert }) => {})